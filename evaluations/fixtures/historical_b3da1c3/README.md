# Historical provenance-alias fixture

This directory freezes the exact pre-fix `arte_cognition/epistemic_memory.py` Git blob from commit `b3da1c3db7eb29995a6d06f696016cf52c89cd77`.

The evaluator verifies the Git-object SHA before use. The later repaired implementation is not used as an oracle, task input, or candidate-generation source. Hidden executable regressions independently test two requirements: preserve all generator ancestry when two proposals realize one exact intervention phenotype, and preserve ordinary revision semantics when the actual intervention differs despite an intentionally colliding experiment ID.
